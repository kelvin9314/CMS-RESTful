apps:
  - script: src/api/server.js
    name: MP-Library-CMS
    instances: 1
    exec_mode: cluster
    watch: false
    env:
      NODE_ENV: development
    env_production:
      NODE_ENV: production
